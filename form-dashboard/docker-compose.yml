
version: '3.8'

services:
  frontend:
    image: form-dashboard-frontend:latest
    ports:
      - "48620:3000"
    environment:
      NODE_ENV: production
      COOKIE_SECURE: 'false'
      NEXT_PUBLIC_API_URL: 'http://187.103.0.135:8000'
      NEXT_PUBLIC_WS_URL: 'ws://187.103.0.135:8000'
      NEXT_PUBLIC_FORM_URL: 'http://forms.newnet.com.br:48625'
    deploy:
      restart_policy:
        condition: on-failure
